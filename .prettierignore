node_modules
dist
fonts
pnpm-lock.yaml

# The decks were never formatted — .eslintignore excluded them, so prettier
# never saw them either. Reformatting them now would rewrite every talk's
# MDX for no benefit, and MDX is easy to break with a stray reflow.
src/talks
